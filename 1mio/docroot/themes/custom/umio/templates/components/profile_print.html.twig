{#
/**
 * @file
 * Default theme implementation for a region.
 *
 * Available variables:
 * - content: The content for this region, typically blocks.
 * - attributes: Remaining HTML attributes for the element, including:
 *   - class: HTML classes that can be used to style contextually through CSS.
 *
 * @see template_preprocess_region()
 *
 * @ingroup themeable
 */
#}

<div class="col-12 col-md-10 offset-md-1 col-lg-6 offset-lg-3 d-flex justify-content-center profile-printer align-items-center">
  <div>
    <h3>
      {{ content.field_user_name[0] }}
    </h3>
    {{ content.field_user_address[0] }}
    <h6>
      {{ userEmail }}
    </h6>
    <h6>
      {{ userPhone }}
    </h6>
  </div>
</div>
