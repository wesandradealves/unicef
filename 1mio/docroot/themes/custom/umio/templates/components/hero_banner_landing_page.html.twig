{#
/**
 * @file
 * Default theme implementation for a region.
 *
 * Available variables:
 * - content: The content for this region, typically blocks.
 * - attributes: Remaining HTML attributes for the element, including:
 *   - class: HTML classes that can be used to style contextually through CSS.
 *
 * @see template_preprocess_region()
 *
 * @ingroup themeable
 */
#}

<section class="hero-banner-landing-page--background-color">
  <div class="hero-banner-landing-page-repeat-x px-md-0">
    <div class="hero-banner-landing-page">

        <div class="col-12 col-xl-6 offset-xl-3 hero-banner-background-bark">

          <div class="content-full">

            {# TEXTS #}
            <div class="content-phrase-and-link">
              <div class="landing-phrase-content">
                <h1>{{ bannerText.textOne }}</h1>
                <h2>{{ bannerText.textTwo }}</h2>
              </div>
            </div>

          </div> {# END CONTENT-FULL #}

          {# BUTTON CONTAINER #}
          <div class="landing-container-btn-link">
            <a href="{{ url('view.feeds_jovens.feed_general') }}" class="btn btn-large landing-btn-link" alt="{{ bannerText.textThree }}">{{ bannerText.textThree }}</a>
          </div>

        </div> {# END background-bark #}

      {# ANIMATIONS #}
      <div class="landingpage-bicycle-container">
        <img class='hero-banner--tree-animation tree-animation-1' src={{'/'~ directory ~ '/images/new_layout/ArvoreTrampo1.png'}} alt='transit_board_animation'>
        <img class='hero-banner--tree-animation tree-animation-2' src={{'/'~ directory ~ '/images/new_layout/ArvoreTrampo2.png'}} alt='transit_board_animation'>
        {{ include('@umio/components/bicycle_animation.html.twig') }}
      </div>
    </div> {# END hero-banner-landing-page #}
  </div>
</section>
